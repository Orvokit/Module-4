/*
Напишите программу, которая вычисляет модуль числа.
Подсказка: чтобы обратить знак числа в переменной ‘x’ надо писать вот так:
x = -x
*/

package main

import (
	"fmt"
)

func main() {
	fmt.Println("Введите число.")
  var x int
  fmt.Scan (&x)
  if x < 0{
    x = -x
  }
  fmt.Println("Модуль вашего числа:", x)
}
